name: Pairwise opposition matched-geometry calibration

on:
  pull_request:
    types: [opened, reopened, synchronize]
    branches: [main]
    paths:
      - 'experiments/opposition_calibration.py'
      - '.github/workflows/opposition_calibration.yml'
  workflow_dispatch:

permissions:
  contents: read

env:
  OMP_NUM_THREADS: '1'
  MKL_NUM_THREADS: '1'
  OPENBLAS_NUM_THREADS: '1'
  NUMEXPR_NUM_THREADS: '1'

jobs:
  pilot:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        start: [2900, 2902, 2904, 2906, 2908]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Pinned CPU dependencies
        run: |
          pip install torch==2.10.0 --index-url https://download.pytorch.org/whl/cpu
          pip install numpy==2.3.5 pandas==2.2.3 scipy==1.17.0 scikit-learn==1.8.0
      - name: Opposition synthetic tests
        run: python experiments/opposition_calibration.py selftest
      - name: Training-reference-only Stage A
        run: python experiments/opposition_calibration.py run --start ${{ matrix.start }} --end $(( ${{ matrix.start }} + 2 )) --output-dir artifact
      - name: Archive source and backend
        run: |
          mkdir -p artifact/source
          cp experiments/*.py artifact/source/
          pip freeze > artifact/pip-freeze.txt
      - uses: actions/upload-artifact@v4
        with:
          name: opposition-pilot-${{ matrix.start }}
          path: artifact/
          retention-days: 90
          if-no-files-found: error

  aggregate:
    needs: pilot
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Pinned validator runtime
        run: |
          pip install torch==2.10.0 --index-url https://download.pytorch.org/whl/cpu
          pip install numpy==2.3.5 pandas==2.2.3 scipy==1.17.0 scikit-learn==1.8.0
      - uses: actions/download-artifact@v4
        with:
          pattern: opposition-pilot-*
          path: downloaded
      - name: Apply frozen Stage-A gates
        run: python experiments/opposition_calibration.py summarize --input-dir downloaded --output-dir aggregate
      - uses: actions/upload-artifact@v4
        with:
          name: opposition-calibration-evidence
          path: |
            aggregate/
            downloaded/
          retention-days: 90
          if-no-files-found: error
